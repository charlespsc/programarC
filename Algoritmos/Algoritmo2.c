#include <stdio.h>

int main() {
    // Declaração das variáveis
    int num1 = 8, num2 = 9, num3 = 7;
    float ma;

    // Cálculo da média
    ma = (num1 + num2 + num3) / 3.0;

    // Impressão do resultado
    printf("A media dos numeros 8, 9 e 7 é: %.2f\n", ma);

    return 0;
}
